{
  "name": "p4-echo-suppressed",
  "description": "Mic picks up the TTS content (\"weather forecast\") while TTS is playing; echo filter suppresses it and no barge fires.",
  "phase": "p4-barge",
  "flags": { "bargeIn": true },
  "steps": [
    { "type": "hook", "name": "ttsStart", "args": { "text": "here is the weather forecast for today and tomorrow" } },
    { "type": "hook", "name": "userSay", "args": { "text": "weather forecast" } },
    { "type": "assert", "metaEquals": { "bargeCount": 0, "echoSuppressCount": 1 } }
  ]
}
